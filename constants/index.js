const GAME_STATE = {
  BAN_PICK_PHASE: "ban/pick phase",
  WAIT_IN_LOBBY: "players waiting in the lobby",
  IN_GAME: "in game",
};

const TICK_RATE = 1000 * 3;

module.exports = {
  GAME_STATE,
  TICK_RATE,
};
